# meu-sinal-amps
# Meu Simulador AMPS Anos 90
Este projeto serve para recriar o sinal analógico de celulares antigos.
git init
git add .
git commit -m "Primeiro envio"
git branch -M main
git remote add origin REGISTRE_AQUI_O_LINK_DO_SEU_GITHUB
git push -u origin main
